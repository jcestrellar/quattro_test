import { Box } from '@mui/material';
import { LaunchIndex } from '../../features/launch/launchIndex';

export const LaunchPage = () => {
  return (
    <Box width={'100vw'} height={'100vh'}>
      <LaunchIndex />
    </Box>
  );
};
